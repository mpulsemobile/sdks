package com.android.mpulseframework.securemessages;

import com.android.mpulseframework.MPulseAppConfig;

/**
 * Model to store the secure message configuration for MPulse
 */
public class SecureMessagesConfig extends MPulseAppConfig {

    /**
     * Constructor of SecureMessagesConfig
     *
     * @param appConfig   the AppConfig object holding the MPulse application configuration {@link MPulseAppConfig}
     */
    public SecureMessagesConfig(MPulseAppConfig appConfig) {
        super(appConfig);
    }
}
